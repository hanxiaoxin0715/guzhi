# FEICAI Studio (Windows 源码版) 使用教程

恭喜您！您已经成功配置并运行了 FEICAI Studio 的源代码版本。本教程将指导您如何日常启动和更新软件。

## 📂 项目目录
- **项目根目录**: `D:\TR\FEICAI-Studio`
- **源代码目录**: `D:\TR\FEICAI-Studio\app`
- **输出文件**: `D:\TR\FEICAI-Studio\outputs` (生成的图片、视频等)

---

## 🚀 如何启动程序

### 方式一：命令行启动（推荐）
1. 打开终端（PowerShell 或 CMD）。
2. 进入源代码目录：
   ```powershell
   cd D:\TR\FEICAI-Studio\app
   ```
3. 启动开发服务器：
   ```powershell
   npm run dev
   ```
4. 等待终端显示 `Ready in ... ms` 后，打开浏览器访问：
   [http://localhost:3000](http://localhost:3000)

---

## 🔄 如何更新版本

由于您使用的是源代码版本，**请勿使用**文件夹中的 `FEICAI-Studio-V1.87.exe` 进行更新。请使用我为您准备的专用更新脚本。

### 更新步骤：
1. **停止当前服务**：如果在运行中，请在终端按 `Ctrl + C` 停止程序。
2. **运行更新脚本**：
   在终端中执行以下命令：
   ```powershell
   D:\TR\update_source.ps1
   ```
3. **脚本会自动执行以下操作**：
   - 检查官方更新服务器（Mac源）。
   - 自动下载最新补丁。
   - 覆盖更新源代码。
4. **重启服务**：
   更新完成后，重新执行启动命令即可体验新版本：
   ```powershell
   cd D:\TR\FEICAI-Studio\app
   npm run dev
   ```

---

## ❓ 常见问题

**Q: 为什么不能用原来的 exe 更新？**
A: 原来的 exe 是 Windows 编译版，与您现在的源代码环境不兼容。使用脚本更新可以确保您的源码环境始终保持最新，且不受 Windows 版限制。

**Q: 启动时报错 "ENOENT: no such file or directory, open 'package.json'"？**
A: 这是因为您没有进入正确的目录。请确保先执行 `cd D:\TR\FEICAI-Studio\app` 再运行 `npm run dev`。

**Q: 更新后需要重新安装依赖吗？**
A: 通常不需要。但如果更新后启动报错提示找不到模块，可以运行 `npm install` 修复。
